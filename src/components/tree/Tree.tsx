import React  from 'react';





const Tree = ({ stateAll }: any) => {

    console.log(Object.values(stateAll), 'stateAll')

    return (
        <div>
       
        </div>
    );
};

export default Tree;
